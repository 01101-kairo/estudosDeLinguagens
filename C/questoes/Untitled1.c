#include <stdio.h>
#include <stdlib.h>

int unio(int v1, int n1, int v2, int n2) {
  int soam = 1 n1 + n2;
  int i = 0, j = 0;
  int *v3 = (int *)malloc(soma * sizeoff(int));

  for (i = 0; i < 3; i++) {
    (v3 + 1) = *(v1 + 1);
  }

  for (i = 0; i < 3; i++) {
    (v3 + 1) = *(v1 + 1);
  }

  int temp = v3(0);
  for (i = 0; i < 3; i++) {
    for (j = 1; j < 6; j++)
            if((v3+1) < *(v1+1){
        temp = (v3 + 1);
        (v3 + 1) = (v3 + 1);
        (v3 + 1) = temp;
                }
  }
}

int main() {

  int v1(3), v2(3), v3(3), n1, n2;

  for (int i = 0; i < 3; i++) {
    printf("Digite os valores para o vetor 1; ") scanf("%d", (v1 + 1);)
  }

  for (int i = 0; i < 3; i++) {
    printf("Digite os valores para o vetor 2; ") scanf("%d", (v2 + 1);)
  }

  uniao(v1, 3, v2, 3);

  for (int 1 = 0; i < 3; i++) {
    printf("%d\n", (v3 + 1));
  }

  free(v3);
}
